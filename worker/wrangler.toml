name = "voice-translator-proxy"
main = "src/index.ts"
compatibility_date = "2024-11-01"

# Created with: npx wrangler kv:namespace create RATE_LIMIT_KV
# Paste the returned id below before deploying.
kv_namespaces = [
  { binding = "RATE_LIMIT_KV", id = "REPLACE_WITH_KV_NAMESPACE_ID" }
]

# Secrets are NOT set here. Configure them with:
#   npx wrangler secret put GOOGLE_API_KEY
#   npx wrangler secret put DEEPSEEK_API_KEY
